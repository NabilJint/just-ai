/** Stable Liveblocks identity for the Ghost AI design agent. */
export const AI_AGENT_USER_ID = "ghost-ai-agent";

export const AI_AGENT_DISPLAY_NAME = "Ghost AI";

export const AI_AGENT_CURSOR_COLOR = "#8b82ff";

export const AI_AGENT_AVATAR_URL = "/favicon.png";

/** Minimum spacing between nodes when placing new elements (px). */
export const CANVAS_LAYOUT_GAP_X = 120;

export const CANVAS_LAYOUT_GAP_Y = 80;

/** Presence TTL refreshed while the agent is working (seconds). */
export const AI_PRESENCE_TTL_ACTIVE = 120;

/** Short TTL when clearing agent presence at the end (seconds). */
export const AI_PRESENCE_TTL_CLEAR = 3;


